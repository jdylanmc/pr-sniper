{
  "name": "pr-sniper",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.20.0 <25"
  },
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build",
    "tauri": "tauri",
    "format": "prettier --write src tests/settings index.html package.json tsconfig.json vite.config.ts src-tauri/tauri.conf.json .github/workflows/macos.yml README.md docs/adr/0001-macos-foundation.md CHANGELOG.md && cargo fmt --manifest-path src-tauri/Cargo.toml",
    "format:check": "prettier --check src tests/settings index.html package.json tsconfig.json vite.config.ts src-tauri/tauri.conf.json .github/workflows/macos.yml README.md docs/adr/0001-macos-foundation.md CHANGELOG.md && cargo fmt --manifest-path src-tauri/Cargo.toml --check",
    "lint": "tsc --noEmit && cargo clippy --manifest-path src-tauri/Cargo.toml --locked --all-targets -- -D warnings",
    "test": "cargo test --manifest-path src-tauri/Cargo.toml --locked",
    "test:settings": "npm run build && cargo build --manifest-path src-tauri/Cargo.toml --locked --example settings_bridge && playwright test --config tests/settings/playwright.config.mjs",
    "bundle": "tauri build --bundles app"
  },
  "dependencies": {
    "@tauri-apps/api": "2.11.1"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "@tauri-apps/cli": "2.11.4",
    "prettier": "3.9.6",
    "typescript": "7.0.2",
    "vite": "8.3.0"
  }
}
