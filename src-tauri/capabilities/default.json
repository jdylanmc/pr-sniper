{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "local-foundation",
  "description": "Only local application windows. No shell, filesystem, or credential access from the webview.",
  "windows": ["queue", "settings", "status", "doctor", "diagnostics"],
  "permissions": []
}
